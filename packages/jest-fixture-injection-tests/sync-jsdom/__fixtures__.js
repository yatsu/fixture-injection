const f = c => `F(${c})`

const g = (c, d) => `G(${c},${d})`

const h = 'H()'

const i = (d, e) => `I(${d},${e})`

const j = e => `J(${e})`

const k = (f, h) => `K(${f},${h})`

const l = (h, j) => `L(${h},${j})`

module.exports = {
  f,
  g,
  h,
  i,
  j,
  k,
  l
}
