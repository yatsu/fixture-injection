{
  "name": "jest-fixture-injection-example-cra",
  "version": "0.1.4",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "test": "craco test --env=jest-fixture-injection/jsdom",
    "test-ci": "CI=1 JEST_JUNIT_OUTPUT=reports/jest/results.xml craco test --coverage --env=jest-fixture-injection/jsdom --ci --reporters=default --reporters=jest-junit --runInBand",
    "eject": "react-scripts eject",
    "pre-commit": "lint-staged && yarn test --coverage"
  },
  "lint-staged": {
    "linters": {
      "*.js": [
        "prettier-eslint --write",
        "eslint",
        "git add"
      ]
    },
    "ignore": [
      "*.config.js",
      ".eslintrc.js"
    ]
  },
  "engines": {
    "node": ">=10"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "dependencies": {
    "@craco/craco": "^5.2.4",
    "react": "^16.6.6",
    "react-dom": "^16.6.6",
    "react-scripts": "3.0.1"
  },
  "devDependencies": {
    "jest-fixture-injection": "^0.1.4",
    "jest-junit": "^6.4.0",
    "lint-staged": "^9.2.0",
    "prettier-eslint-cli": "^5.0.0"
  }
}
